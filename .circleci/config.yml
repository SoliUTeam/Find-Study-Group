version: 2.1

orbs:
  ruby: circleci/ruby@1.2.0
  
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run:
          name: Bundle Installation check
          command: |
            bundle install
            echo 'Bundle Installation Check Done!'
      
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
      - run:
          name: Code Has Arrived
          command: |
            ls -al
            echo '^^^That should look familiar^^^'
